# cd /dev (" Change directory to dev directory")
# ls (" list archives and directories ")
# Partitioning disks in Linux: SDA and SDB 
#------------------------------------------------------------------------------------------
# ^C  ("is used to terminate or stop any running process  ")
# cd /proc (" Change directory to proc directory") 
# ls ("list procces in proc directory")
# cat cpuinfo (" show cpu information")
#------------------------------------------------------------------------------------------
# cd / ("This command helps us move to the root directory of the filesystem")
# ls  ("This command helps us list the files and directories in the current directory")
#------------------------------------------------------------------------------------------
# cd /bin ("This command helps us move to the bin directory, which contains essential command binaries that are necessary for the system to function properly")
# ls ("This command helps us list the files and directories in the current directory, which in this case is the bin directory")
#------------------------------------------------------------------------------------------
# cd/workspaces/UNIX-02-SIN-C-Mar-Jul-2026 ("This command helps us move to the directory where we have our codespace")
# gcc saludo.c -o saludo ("This command compiles the C file named saludo.c and creates an executable file named saludo")
#------------------------------------------------------------------------------------------
# mv ("move or rename files and directories")
# cp ("copy files and directories")
# sudo mv saludo_bin /bin ("This command moves the compiled file named saludo to the bin directory, which allows us to run the program from anywhere in the terminal")
# ls | grep saludo ("This command helps us check if the file named saludo is in the bin directory")
#------------------------------------------------------------------------------------------
# pwd ("This command helps us check the current directory we are in")
#------------------------------------------------------------------------------------------
# ls -F ("This command helps us list the files and directories in the current directory, and it adds a symbol at the end of each name to indicate the type of file or directory.")
# ls -i ("This command helps us list the files and directories in the current directory, and it also shows the inode number of each file or directory. The inode number is a unique identifier for each file or directory in the filesystem.")
#------------------------------------------------------------------------------------------
# echo "Silencio Keyla" > test.txt ("This command creates a file named test.txt and writes the text "Silencio Keyla" into it")
# cat test.txt ("This command helps us check the content of the file named test.txt")
# stat test.txt ("This command provides detailed information about the file named test.txt, including its size, permissions, and timestamps.")